spring:
  application:
    name: apimentejuego

  datasource:
    url: jdbc:postgresql://localhost:6969/mentejuegodb
    driver-class-name: org.postgresql.Driver
    username: admin
    password: admin

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect